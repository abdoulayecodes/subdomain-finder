# subdomain-finder
Python script that enumerates subdomains with sockets and multithreading
## Installation
```bash
git clone https://github.com/abdoulayecodes/subdomain-finder.git
cd subdomain-finder
```
